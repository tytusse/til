# "perhaps I should use some of the following practices?"
- https://github.com/bgaillard/readonly.nvim - automatically makes files as readonly and modifiable=false,
  displays red info about file being recognized as "sensitive".
  Allows configuring patterns to match sensitive files by path (at least).
