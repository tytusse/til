Use `:help config` to see paths where file should be.

For win it will be (using powershell notation): `$env:LOCALAPPDATA\nvim` (i.e. `C:\Users\tomas\AppData\Local\nvim`)

For the moment config has bare essentials like clipboard, line numbers and tabs-as-spaces.

[Used cfg](simplistic-init.lua)
